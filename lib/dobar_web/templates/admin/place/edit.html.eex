<h1>Edit Place</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.admin_place_path(@conn, :update, @place)) %>

<span><%= link "Back", to: Routes.admin_place_path(@conn, :index) %></span>
