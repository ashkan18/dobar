<h1>Edit Place image</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.admin_place_place_image_path(@conn, :update, @place_image)) %>

<span><%= link "Back", to: Routes.admin_place_place_image_path(@conn, :index, @place_image.place_id)  %></span>
