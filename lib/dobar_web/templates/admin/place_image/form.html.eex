<%= form_for @changeset, @action, [multipart: true], fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :image_file %>
  <%= file_input f, :image_file %>
  <%= error_tag f, :image_file %>

  <div>
    <%= submit "Upload" %>
  </div>
<% end %>
